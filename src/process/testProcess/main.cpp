#include<iostream>

using namespace std;

int main()
{
	/*string input;
	do {
		cin >> input;
		cout << "sub-process input: " << input << endl << flush;
	} while (0 != input.compare("exit"));*/
	cout << "Hello world!" << flush << endl;
	return 0;
}